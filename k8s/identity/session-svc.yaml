---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: session-svc
  namespace: minipay
spec:
  replicas: 1
  selector:
    matchLabels:
      app: session-svc
  template:
    metadata:
      labels:
        app: session-svc
    spec:
      containers:
        - name: session-svc
          image: ghcr.io/arobertm/minipay/session-svc:10333c9ba2da00e291bbec15a40d1a5e2a9c5302
          imagePullPolicy: Always
          ports:
            - name: http
              containerPort: 8083
          env:
            - name: SERVER_PORT
              value: "8083"
            - name: SPRING_MAIN_LAZY_INITIALIZATION
              value: "true"
            - name: MINIDS_ADDRESS
              value: http://minids-api:8301
            - name: AUTH_JWK_URI
              value: http://auth-svc:8081/oauth2/jwks-rs256
            - name: AUTH_ISSUER_URI
              value: http://auth-svc:8081
            - name: JAVA_TOOL_OPTIONS
              value: "-Xms32m -Xmx128m -XX:MaxMetaspaceSize=96m -XX:ReservedCodeCacheSize=24m -XX:TieredStopAtLevel=1"
          startupProbe:
            httpGet:
              path: /actuator/health
              port: 8083
            failureThreshold: 24
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: 8083
            periodSeconds: 10
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: /actuator/health
              port: 8083
            periodSeconds: 15
            failureThreshold: 3
          resources:
            requests:
              memory: 64Mi
              cpu: 75m
            limits:
              memory: 320Mi
              cpu: 300m

---
apiVersion: v1
kind: Service
metadata:
  name: session-svc
  namespace: minipay
spec:
  selector:
    app: session-svc
  ports:
    - port: 8083
      targetPort: 8083
