---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: psd2-svc
  namespace: minipay
spec:
  replicas: 1
  selector:
    matchLabels:
      app: psd2-svc
  template:
    metadata:
      labels:
        app: psd2-svc
    spec:
      containers:
        - name: psd2-svc
          image: ghcr.io/arobertm/minipay/psd2-svc:10333c9ba2da00e291bbec15a40d1a5e2a9c5302
          imagePullPolicy: Always
          ports:
            - name: http
              containerPort: 8095
          env:
            - name: SERVER_PORT
              value: "8095"
            - name: JAVA_TOOL_OPTIONS
              value: "-Xms64m -Xmx200m -XX:MaxMetaspaceSize=128m"
          startupProbe:
            httpGet:
              path: /actuator/health
              port: 8095
            failureThreshold: 30
            periodSeconds: 10
            timeoutSeconds: 5
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: 8095
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: /actuator/health
              port: 8095
            periodSeconds: 20
            timeoutSeconds: 5
            failureThreshold: 3
          resources:
            requests:
              memory: 256Mi
              cpu: 100m
            limits:
              memory: 512Mi
              cpu: 300m

---
apiVersion: v1
kind: Service
metadata:
  name: psd2-svc
  namespace: minipay
spec:
  selector:
    app: psd2-svc
  ports:
    - port: 8095
      targetPort: 8095
