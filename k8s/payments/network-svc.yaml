---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: network-svc
  namespace: minipay
spec:
  replicas: 1
  selector:
    matchLabels:
      app: network-svc
  template:
    metadata:
      labels:
        app: network-svc
    spec:
      containers:
        - name: network-svc
          image: ghcr.io/arobertm/minipay/network-svc:10333c9ba2da00e291bbec15a40d1a5e2a9c5302
          imagePullPolicy: Always
          ports:
            - name: http
              containerPort: 8086
          env:
            - name: SERVER_PORT
              value: "8086"
            - name: VAULT_URL
              value: http://vault-svc:8085
            - name: ISSUER_URL
              value: http://issuer-svc:8087
            - name: JAVA_TOOL_OPTIONS
              value: "-Xms32m -Xmx128m -XX:MaxMetaspaceSize=96m -XX:+UseSerialGC"
          startupProbe:
            httpGet:
              path: /actuator/health
              port: 8086
            failureThreshold: 24
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: 8086
            periodSeconds: 10
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: /actuator/health
              port: 8086
            periodSeconds: 15
            failureThreshold: 3
          resources:
            requests:
              memory: 64Mi
              cpu: 75m
            limits:
              memory: 192Mi
              cpu: 300m

---
apiVersion: v1
kind: Service
metadata:
  name: network-svc
  namespace: minipay
spec:
  selector:
    app: network-svc
  ports:
    - port: 8086
      targetPort: 8086
